# dependencies
/node_modules
/.pnp
.pnp.js

# next.js
/.next/
/out/
next-env.d.ts

# LOCAL DATA — contains proxy credentials, account sessions/cookies, the SQLite
# DB, and per-profile browser user-data-dirs. NEVER commit this.
/data/

# env / secrets
.env
.env*.local

# one-click starter scripts hand-built per teammate — these bake in a live
# DATABASE_URL, never let one reach the (public) remote
start-mac.command
start-*.command

# misc
.DS_Store
*.pem
npm-debug.log*

# build artifact
/tsconfig.tsbuildinfo
